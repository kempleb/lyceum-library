# Apology — pipeline manifest (Stephanus, generated for the bookless rollout)
#
# Plato is cited by Stephanus page + section (17a, ... 42a), not
# by Bekker page. See manifests/Euthyphro.yaml for the full scheme-contract
# commentary (stage1/stage2/stage7 dispatch on citation.scheme).

# NOTE: this file is a shape template (it illustrates the manifest schema for
# a Stephanus-scheme work) — it is not itself consumed by the pipeline.
work:
  id: Apology
  title: "Apology"
  author: "plato"       # author slug (lowercase), used to group works by author
  language: "grc"        # 'grc' (default) or 'lat'; Latin is parameterized but
                          # not yet implemented (Wave 2) — see stage4/stage5.
  tlg_author: "0059"
  tlg_work: "002"
  greek_edition: "Burnet, Platonis Opera vol. 1 (OCT, 1900)"

citation:
  scheme: stephanus
  hideLineNumbers: true

# No canonical speaker-label map filled in for this rollout pass — labels
# render as-is (empty map is fine per the scheme contract).
speakers: {}

english:
  primary:
    id: fowler
    name: "H. N. Fowler (Loeb, 1914)"
    model: perseus_stephanus
    file: "perseus-eng/tlg0059.tlg002.perseus-eng2.xml"

# Verified, edition-faithful section gaps: none declared yet for this
# generated pass (stage2's observed-schemed validator demotes intra-column
# line gaps for stephanus automatically; report any REAL column-order gaps
# stage2 surfaces back for review).
expected_section_gaps: []
section_spine: { count: 125, sha256: 9cdd581df4802592bb496a16c1c9a690100b19012c2e9029f1a6b573d69f69ef }

# The Stephanus span from docs/registry-draft.md: 17a-42a.
# Single, undivided work -> one book. Start/end use line 1 / line 99 as safe
# outer bounds (book_for_line only needs to bracket the observed spine; lines
# restart per section and are not user-facing under this scheme).
books:
  - { n: 1, start: "17a1", end: "42a99" }

sources:
  tlg_dir_env: "TLG_DIR"
  tlg_dir_default: "../TLG Files/TLG"
  diogenes_server: "/Applications/Diogenes.app/Contents/server"
  diogenes_data: "/Applications/Diogenes.app/Contents/dependencies/data"
